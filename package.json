{
  "name": "simple-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js app for Docker deployment",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"No test specified\" && exit 0" // Replace with actual tests if available
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
